Just run label_faces.py

There is no way to save your work in progress, so plan on doing it in one sitting.
Saves the results to a csv.

Should run straight out of the box, but has only been tested on windows.

You will need pandas and opencv libraries
If you don't have them, you can run the following in the terminal:
    pip install opencv-python
    pip install pandas
